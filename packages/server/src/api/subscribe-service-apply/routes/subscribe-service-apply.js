'use strict';

/**
 * subscribe-service-apply router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscribe-service-apply.subscribe-service-apply');
