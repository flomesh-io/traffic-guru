'use strict';

/**
 * subscribe-service-apply controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::subscribe-service-apply.subscribe-service-apply');
